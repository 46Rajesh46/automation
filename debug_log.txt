1. Prompt to paste in Claude Code on the server
Copy-paste this exactly into Claude Code on the server:


The project is at D:\automation-project - Main\core.py

Make these 3 fixes to core.py:

FIX 1 — In the function that toggles Tools after a report download (around the block that clicks MainMenuLinksTools 3 times), the success path never calls driver.switch_to.default_content(). Add it right before the print("[OK] Tools toggled after report download") line.

FIX 2 — In process_unit, the try block that clicks MainMenuSessionBarLocation starts with driver.switch_to.frame("eprmenu"). Add driver.switch_to.default_content() as the very first line inside that try block (before the frame switch).

FIX 3 — In process_unit, between the try/except block that opens the unit selector and the for attempt in range(8) loop, add this wait:
    try:
        wait.until(EC.presence_of_element_located((By.XPATH, "//*[contains(text(),'Choose Logon Location')]")))
    except:
        pass

FIX 4 — In get_driver(), after the options.add_argument lines, add:
    _ext_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), "fix2_edge_extension")
    if os.path.isdir(_ext_path):
        options.add_argument(f"--load-extension={_ext_path}")

After making all 4 fixes, show me the changed lines with line numbers so I can verify.
fix all errors
